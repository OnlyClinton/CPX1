"use client";

// The conversion hierarchy is authored directly in each page. Keeping this
// compatibility component avoids a second runtime system mutating CTAs or
// injecting device-specific action docks.
export default function FinalCTAContract(){return null}
