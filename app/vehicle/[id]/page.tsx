"use client";import Link from"next/link";import{useEffect,useState}from"react";import{Footer,Header}from"../../components";import{fallbackVehicle}from"../../../lib/publicInventoryFallback";
export default function Vehicle({params}:{params:Promise<{id:string}>}){const[id,setId]=useState(""),[v,setV]=useState<any>(),[loading,setLoading]=useState(true);useEffect(()=>{params.then(x=>setId(x.id))},[params]);useEffect(()=>{if(!id)return;const fallback=fallbackVehicle(id);if(fallback)setV(fallback);fetch(`/api/inventory/${encodeURIComponent(id)}`,{cache:"no-store"}).then(r=>r.json()).then(j=>{if(j.item)setV(j.item)}).catch(()=>{}).finally(()=>setLoading(false))},[id]);const q=id?`?vehicle=${encodeURIComponent(id)}&source=vdp`:"";return <><Header/><main className="section light"><div className="wrap">{v?<><div className="eyebrow muted">VEHICLE DETAILS</div><h2>{v.year} {v.make} {v.model}</h2><div className="grid" style={{gridTemplateColumns:"1.4fr .8fr"}}><div className="photo" style={{borderRadius:22}}>{v.primaryPhotoPathname?<img src={`/api/media?p=${encodeURIComponent(v.primaryPhotoPathname)}`} alt={`${v.year} ${v.make} ${v.model}`}/>:v.primary_image_url?<img src={v.primary_image_url} alt={`${v.year} ${v.make} ${v.model}`}/>:"PHOTOS COMING"}</div><div><div className="price">${Number(v.price||0).toLocaleString()}</div>{(v.downPayment??v.down_payment)!=null&&<div className="down">${Number(v.downPayment??v.down_payment).toLocaleString()} estimated down</div>}<p>{Number(v.mileage||0).toLocaleString()} miles</p><p>{v.description}</p><div className="actions"><Link className="cta red" href={`/schedule-test-drive${q}`}>SCHEDULE TEST DRIVE</Link><Link className="cta" href={`/get-approved${q}`}>GET APPROVED</Link><a className="cta ghost" href="tel:+18135164752">CALL SEAN</a></div></div></div></>:loading?<h2>Loading vehicle…</h2>:<><h2>Vehicle not found.</h2><p>Call Sean at 813-516-4752 for current availability.</p></>}</div></main><Footer/></>}
