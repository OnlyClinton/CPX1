name: WDCC WEBGPU CINEMATIC PREVIEW V1 — RELEASE FREEZE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Block non-release preview deployment
        run: |
          echo 'WebGPU preview deployment is frozen during WDCC final certification.'
          echo 'No Vercel deployment was requested.'
