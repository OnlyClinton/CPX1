name: WDCC APEX RESTORE — FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Release-control guard
        run: |
          echo 'Apex/www production deployment is frozen during immutable candidate acceptance.'
          echo 'This workflow cannot checkout moving main, deploy production, or move customer aliases.'
          echo 'Frozen candidate: 188392c81696da5d0d61f5c6f0bec45f490a232d'
