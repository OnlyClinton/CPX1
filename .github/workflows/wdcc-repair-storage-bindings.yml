name: WDCC STORAGE BINDING REPAIR — FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Release-control guard
        run: |
          echo 'Legacy storage-binding repair is frozen.'
          echo 'It may not mutate Phoenix environment variables, redeploy legacy artifacts, or move dealer aliases during immutable candidate acceptance.'
          echo 'Use wdcc-safe-candidate-repair-cleanup.yml instead.'
