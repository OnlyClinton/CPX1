name: WDCC CLOUDFLARE DEALER LIVE — RETIRED

# RETIRED 2026-08-26.
# This legacy workflow previously proxied dealer API traffic through the customer
# storefront and could overwrite the canonical split-origin dealer front door.
# The ONLY live owner is .github/workflows/wdcc-dealer-target-frontdoor-v3.yml.
# Dealer UI is served by wdcc-dealer-portal.vercel.app and dealer APIs are served
# by the immutable Phoenix authority wdcc-cpx-launch-qhcvflfih-cpxagency.vercel.app.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse legacy dealer-frontdoor mutation
        run: |
          echo 'RETIRED: use wdcc-dealer-target-frontdoor-v3.yml. No live mutation performed.' >&2
          exit 1
