name: WDCC 2VFD APEX PIN — FROZEN
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo '2vfD apex pin is frozen; no customer-domain alias mutation is permitted during candidate acceptance.'
