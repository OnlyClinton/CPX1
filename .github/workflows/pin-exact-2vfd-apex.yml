name: WDCC 2VFD APEX PIN — FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Release-control guard
        run: |
          echo 'Exact 2vfD apex/www pin is frozen during immutable candidate acceptance.'
          echo 'No customer alias mutation is permitted from this workflow.'
          echo 'Current approved storefront donor remains exact 2vfD; no action is taken.'
