name: WDCC 2VFD PRODUCTION FINALIZER — FROZEN
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo '2vfD production finalizer is frozen; no customer alias or production-domain mutation is permitted.'
