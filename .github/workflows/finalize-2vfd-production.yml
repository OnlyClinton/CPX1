name: WDCC CANONICAL PRODUCTION PROMOTION — FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Release-control guard
        run: |
          echo 'Canonical storefront promotion is frozen during immutable candidate acceptance.'
          echo 'No Vercel promote, alias change, or customer-domain cutover is permitted from this workflow.'
