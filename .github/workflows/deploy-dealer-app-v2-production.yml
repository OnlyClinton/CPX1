name: WDCC DEALER PRODUCTION DEPLOY — FROZEN
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Dealer production deploy is frozen; this branch cannot deploy or move dealer.wedontcarecars.com.'
