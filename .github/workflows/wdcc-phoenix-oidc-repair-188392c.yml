name: WDCC PHOENIX OIDC REPAIR — FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Release-control guard
        run: |
          echo 'Legacy Phoenix OIDC repair is frozen.'
          echo 'It may not remove production Blob credentials, redeploy through production authority, or reset aliases during candidate acceptance.'
          echo 'Use wdcc-safe-candidate-repair-cleanup.yml instead.'
