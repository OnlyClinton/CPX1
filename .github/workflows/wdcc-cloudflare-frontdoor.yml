name: WDCC CLOUDFLARE FRONTDOOR — FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Release-control guard
        run: |
          echo 'Cloudflare front-door deployment is frozen during immutable candidate acceptance.'
          echo 'No Worker deploy, DNS change, alias move, or customer-domain cutover is permitted from this workflow.'
          echo 'Frozen candidate: 188392c81696da5d0d61f5c6f0bec45f490a232d'
