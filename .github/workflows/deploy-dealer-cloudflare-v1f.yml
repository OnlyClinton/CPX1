name: WDCC DEALER CLOUDFLARE V1F — FROZEN
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy dealer Cloudflare cutover is frozen during immutable candidate acceptance; no Worker, route, DNS, or customer-domain mutation is permitted.'
