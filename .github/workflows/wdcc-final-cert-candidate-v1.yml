name: WDCC Final Cert Candidate V1 — QUOTA HOLD

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Final-cert deployment is intentionally held while Vercel api-deployments-free-per-day is active.'
          echo 'Exact current source: 7c93e998d2661d78cdc295239132f75fbaf7520c'
          echo 'Source build proof: issue #382.'
          echo 'Phoenix OIDC presigned Blob proof: issue #385.'
          echo 'No deployment or alias movement is performed by this hold workflow.'
