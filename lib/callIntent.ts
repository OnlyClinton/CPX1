import crypto from "node:crypto";
import {get,list,put} from "@vercel/blob";
export type CallIntent={tenantId?:string|null;sessionId?:string|null;anonymousUserId?:string|null;vehicleId?:string|null;source?:string|null;medium?:string|null;campaign?:string|null;content?:string|null;term?:string|null;clickId?:string|null;referralCode?:string|null;pagePath?:string|null;landingPath?:string|null;referrer?:string|null;cta?:string|null;};
const opt=()=>process.env.BLOB_READ_WRITE_TOKEN?{token:process.env.BLOB_READ_WRITE_TOKEN}:{};const clean=(v:unknown,max=500)=>String(v??"").trim().slice(0,max);
export async function recordCallIntent(input:CallIntent){const at=new Date().toISOString();const id=crypto.randomUUID();const record={id,at,tenantId:clean(input.tenantId||"wdcc",120)||"wdcc",sessionId:input.sessionId?clean(input.sessionId,160):null,anonymousUserId:input.anonymousUserId?clean(input.anonymousUserId,160):null,vehicleId:input.vehicleId?clean(input.vehicleId,160):null,source:input.source?clean(input.source,120):null,medium:input.medium?clean(input.medium,120):null,campaign:input.campaign?clean(input.campaign,160):null,content:input.content?clean(input.content,160):null,term:input.term?clean(input.term,160):null,clickId:input.clickId?clean(input.clickId,220):null,referralCode:input.referralCode?clean(input.referralCode,160):null,pagePath:input.pagePath?clean(input.pagePath,300):null,landingPath:input.landingPath?clean(input.landingPath,300):null,referrer:input.referrer?clean(input.referrer,700):null,cta:input.cta?clean(input.cta,100):"call-sean"};const pathname=`private/logs/call-intent/${at.slice(0,10)}/${at.replace(/[:.]/g,"-")}-${id}.json`;await put(pathname,JSON.stringify(record)+"\n",{access:"private",addRandomSuffix:false,allowOverwrite:false,contentType:"application/json",...opt()});console.log("WDCC_CALL_INTENT",JSON.stringify(record));return record;}
async function readJson(pathname:string){const response=await get(pathname,{access:"private",useCache:false,...opt()});if(!response||response.statusCode!==200||!response.stream)return null;const chunks:Uint8Array[]=[];for await(const chunk of response.stream as any)chunks.push(chunk);return JSON.parse(Buffer.concat(chunks).toString("utf8"));}
export async function readRecentCallIntents(minutes=10,max=100){const cutoff=Date.now()-Math.max(1,Math.min(minutes,120))*60_000;const result=await list({prefix:"private/logs/call-intent/",limit:1000,...opt()});const blobs=[...result.blobs].sort((a:any,b:any)=>String(b.uploadedAt||"").localeCompare(String(a.uploadedAt||""))).slice(0,Math.max(1,Math.min(max,500)));const items:any[]=[];for(const blob of blobs){try{const item=await readJson(blob.pathname);if(item&&Date.parse(item.at)>=cutoff)items.push(item)}catch{}}return items.sort((a,b)=>String(b.at||"").localeCompare(String(a.at||"")));}
